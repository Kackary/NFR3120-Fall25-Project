<h1 class="dashboard-title">Task Tracker Dashboard</h1>

<div class="centered-button">
  <a href="/add" class="btn">➕ Add New Task</a>
</div>
<link rel="stylesheet" href="/style.css">
<table class="task-table">
  <thead>
    <tr>
      <th colspan="4">Task Tracker Dashboard</th>
    </tr>
    <tr>
      <th>Task</th>
      <th>Due Date</th>
      <th>Status</th>
      <th>Actions</th>
    </tr>
  </thead>

  <tbody>
    <% if (tasks.length === 0) { %>
      <tr>
        <td colspan="4" class="empty">No tasks yet. Add one!</td>
      </tr>
    <% } else { %>
      <% tasks.forEach(task => { %>
        <tr>
          <td><%= task.title %></td>
          <td><%= task.dueDate || "No due date" %></td>
          <td><%= task.completed ? "✔ Completed" : "🕒 Pending" %></td>
          <td>
            <a href="/edit/<%= task._id %>" class="edit">Edit</a> |
            <a href="/delete/<%= task._id %>" class="delete">Delete</a>
          </td>
        </tr>
      <% }) %>
    <% } %>
  </tbody>
</table>